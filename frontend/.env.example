# Backend URL — no trailing slash.
# Local: http://localhost:3002
# Production (Render): https://coach-ai-assistant-backend.onrender.com
VITE_API_URL=http://localhost:3002

# Must match DASHBOARD_TOKEN in backend .env.
VITE_DASHBOARD_TOKEN=replace-me-with-32-random-bytes
